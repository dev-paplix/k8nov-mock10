apiVersion: v1
kind: Pod
metadata:
  name: critical-service
spec:
  containers:
  - name: critical-service
    image: ubuntu:latest
    ports:
    - containerPort: 80
  tolerations:
  - key: "key"
    operator: "Equal"
    value: "dedicated"
    effect: "NoSchedule"